@if (TempData["AlertMsgSuccess"] != null)
{
    <div class="alert alert-success alert-dismissible fade-out-toggle" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        @TempData["AlertMsgSuccess"].ToString()
    </div>
}
@if (TempData["AlertMsgInfo"] != null)
{
    <div class="alert alert-info alert-dismissible fade-out-toggle" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        @TempData["AlertMsgInfo"].ToString()
    </div>
}
@if (TempData["AlertMsgWarning"] != null)
{
    <div class="alert alert-warning alert-dismissible fade-out-toggle" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        @TempData["AlertMsgWarning"].ToString()
    </div>
}
@if (TempData["AlertMsgDanger"] != null)
{
    <div class="alert alert-danger alert-dismissible fade-out-toggle" role="alert">
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
        @TempData["AlertMsgDanger"].ToString()
    </div>
}